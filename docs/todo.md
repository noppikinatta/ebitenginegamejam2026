# dev todo

## ~~ポーズ画面のタイルに強化回数を表示~~ ✅ 対応済み
「+1」「+2」といった表示を追加する。
→ `drawTileLevels`：ポーズ（上向き）表示で `Weapon.Level>0` の武器タイル右下に金色の「+N」バッジ（バレルより前面）。説明パネルの武器名にも「+N」を付与。

## ~~ロックオンするタイプの砲塔が対象のほうを向くようにする~~ ✅ 対応済み
というよりも、発射方向を向くといった方が正しい。
→ `Weapon.aimRender`（毎tick `weaponAim` へ平滑化）＋`RenderAngle()`。戦闘描画では各バレルが自分の照準方向（ロックオンは標的、Gatling=前方、Grenade=外側）を向く。ポーズ画面のカット表示は従来どおり上向き（`drawTurretTiles` の `aimBarrels` フラグ）。

## ~~常に上を向く砲塔(ジャンク)表示の追加~~ ✅ 対応済み
五重塔などは縦に長いものであるため、常に上を向いた砲塔のような描画ロジックにすることで表現したい。
→ `AimMode` ではなく `Junk.Tall` フラグ（描画属性）で実装。AimModeは武器の発射方向専用でジャンクに届かず、かつ全て戦車/敵相対なのに対し「常に上」はワールド固定の描画の話のため。`tallJunkNames`（現状 "Five-storied Pagoda"）→ `newJunk`/`randomJunk` で付与。描画はバレルパスを流用し `tallTileSprite` で tall ジャンクの縦長スプライト（`junk_tower`）を angle 0（常に上）で描く。

## ~~アウトゲームの充実~~ ✅ 一通り対応済み
オープニングデモやタイトル、リザルトなどの追加。
→ `scene/opening.go`（エイリアン徘徊→自機登場→博士が武装を次々装着→タイトルへ）、`scene/result.go`（勝敗分岐＋ボタン）、`InGame.Outcome()`。テロップは日本語ハードコード。タイトル画像は `title.png`（ダミー）。残：実アート/テロップ演出の作り込み、BGM/SE。